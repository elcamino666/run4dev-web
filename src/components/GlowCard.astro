---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    'glow-card rounded-2xl bg-bg-surface border border-border p-8 md:p-10 transition-all duration-300 hover:border-accent/30 hover:shadow-sm',
    className,
  ]}
>
  <slot />
</div>
